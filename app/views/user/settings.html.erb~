<% if flash[:notice] %>
	<%= flash[:notice] %>
<% end %>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
  
	<%= devise_error_messages! %><br>

	<div>
	<%= I18n.t('settings.email') %><br>
	<%= f.text_field :email, :value => current_user.email  %><br>

	<%= I18n.t('settings.firstname') %><br>
	<%= f.text_field :first_name, :value => current_user.first_name %><br>
	
	<%= I18n.t('settings.lastname') %><br>
	<%= f.text_field :last_name, :value => current_user.last_name %><br>

  <%= f.label "Neues Passwort" %><br />
  <%= f.password_field :password, :autocomplete => "off" %></div>

  <div><%= f.label "Wiederhole Passwort" %><br />
  <%= f.password_field :password_confirmation %></div>

  <div><%= f.label "aktuelles Passwort" %><br />
  <%= f.password_field :current_password %></div>

  <div><%= f.submit "Update" %></div>
<% end %>


Profilbild:
		<div>
				Profilbild hochladen:
				to do
		</div>


		<div>
			<%= form_for current_user, :url => { :action => "edit_email", :controller => "user" } do |f| %>

				<%= I18n.t('settings.email') %>
				<%= f.text_field :email, :value => current_user.email  %>

				<%= f.submit "E-Mail ändern" %>
			<% end %>
		</div>

		<div>
			<%= form_for current_user, :url => { :action => "edit_name", :controller => "user" } do |f| %>
				<%= I18n.t('settings.firstname') %><%= f.text_field :first_name, :value => current_user.first_name %><br>
				<%= I18n.t('settings.lastname') %><%= f.text_field :last_name, :value => current_user.last_name %> 

				<%= f.submit "Name ändern" %>

			<% end %>
		</div>

<% if flash[:notice] %>
	<%= flash[:notice] %>
<% end %>

