Eigene Anfragen <br><br>

<% if @requests.present? %>
	<% @requests.each do |r| %>
		<%= form_for r, :url => friend_delete_friendship_request_path do |f| %>
			<%= f.hidden_field :id, :value => r.id %>
			<%= r.name %>
			<%= f.submit "Zurückziehen", :id => "deleteRequest" %>
		<% end %>
	<% end %>
<% end %>
<br>
Under Construction!
